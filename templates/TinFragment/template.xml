<?xml version="1.0"?>
<template
    format="5"
    revision="1"
    name="Tin Fragment"
    minApi="9"
    minBuildApi="14"
    description="一键创建 TinMvp Fragment 单个页面所需要的全部组件">

    <category value="Fragment" />
    <formfactor value="Mobile" />

<parameter
        id="pageName"
        name="Page Name"
        type="string"
        constraints="unique|nonempty"
        default="Home"
        help="请填写页面名,如填写 Home,会自动生成 HomeActivity, HomePresenter 等文件" />

<parameter
		id="packageName"
		name="Root Package Name"
		type="string"
		constraints="package"
		default="com.mycompany.myapp"
		help="请填写你的项目包名,请认真核实此包名是否是正确的项目包名,不能包含子包,正确的格式如:me.SouShin.demo"/>
		
<parameter
        id="needFragment"
        name="Generate Fragment"
        type="boolean"
        default="true"
        help="是否需要生成 Fragment ? 不勾选则不生成" /> 

<parameter
        id="fragmentLayoutName"
        name="Fragment Layout Name"
        type="string"
        constraints="layout|nonempty"
        suggest="fragment_${classToResource(pageName)}"
        default="fragment_home"
        visibility="needFragment"
        help="Fragment 创建之前需要填写 Fragment 的布局名,若布局已创建就直接填写此布局名,若还没创建此布局,请勾选下面的单选框" /> 

<parameter
        id="generateFragmentLayout"
        name="Generate Fragment Layout"
        type="boolean"
        default="true"
        visibility="needFragment"
        help="是否需要给 Fragment 生成布局? 若勾选,则使用上面的布局名给此 Fragment 创建默认的布局" />


<parameter
        id="fragmentPackageName"
        name="Fragment Package Name"
        type="string"
        constraints="package"
       suggest="${packageName}.${pageName}" 
        visibility="needFragment"
        help="Fragment 将被输出到此包下,请认真核实此包名是否是你需要输出的目标包名"
        />  
		
<parameter
        id="needView"
        name="Generate View"
        type="boolean"
        default="true"
		help="是否需要生成 View ? 不勾选则不生成" />  	
		
 <parameter
        id="viewPackageName"
        name="View Package Name"
        type="string"
        constraints="package"
        suggest="${packageName}.${pageName}" 
        visibility="needView"
        help="View 将被输出到此包下,请认真核实此包名是否是你需要输出的目标包名"/>  
		
<parameter
        id="needPresenter"
        name="Generate Presenter"
        type="boolean"
        default="true"
        help="是否需要生成 Presenter ? 不勾选则不生成" /> 
		
 <parameter
        id="presenterPackageName"
        name="Presenter Package Name"
        type="string"
        constraints="package"
        suggest="${packageName}.${pageName}"
        visibility="needPresenter"
        help="Presenter 将被输出到此包下,请认真核实此包名是否是你需要输出的目标包名"/>  
		
    <!-- 128x128 thumbnails relative to template.xml -->
    <thumbs>
        <!-- default thumbnail is required -->
        <thumb>template_tin_activity.png</thumb>
    </thumbs>

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>
